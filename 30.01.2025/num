#include <iostream> 
int main() { 
    int num, b, c, d, e; 
    std::cin>>a; 
    b = num % 10; 
    c = num % 100 / 10; 
    d = num % 1000 / 100; 
    e = num / 1000; 
    std::cout << b + c + d + e; 
    return 0; 
}
