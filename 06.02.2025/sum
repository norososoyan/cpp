#include <iostream>
int main() {
    int num;
    int sum = 0;
    std::cout << "Enter the number: ";
    std::cin >> num;
    while (num > 0) {
        sum += num % 10;
        num /= 10;
    }
    std::cout << "The sum is " << sum << std::endl;
    return 0;
}
